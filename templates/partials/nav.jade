
//- Declaration
mixin toc(t)
  if t
    - each item, anchor in t
      li
        if Object.keys(item).length == 0
          a(href="#" + anchor.replace(/ /g,'-').toLowerCase())
            != anchor
        else
          a(href="#" + anchor.replace(/ /g,'-').toLowerCase())
            != anchor
          ul.nav
            +toc(item)

div.hidden-print.affix.side-navbar(role="complementary", id="sidenavbar")
  ul.nav
    +toc(page.metadata.toc)



